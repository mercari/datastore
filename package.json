{
  "name": "datastore",
  "version": "0.5.1",
  "private": true,
  "scripts": {
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s"
  },
  "license": "MIT",
  "devDependencies": {
    "conventional-changelog-cli": "^1.3.4"
  }
}
